#include <iostream>

#include<unordered_map>

using namespace std;

//--->verdict: use unordered maps wherever possible, unless i need a sorted data_structure, in which case, why not just use vectors and pairs???
/*basically, this is a HASHMAP! (but for c++)
the elements are stored in an unorderly fashion (in the map they were sorted into a tree...)*/

int main(){
    //lmao, how do use unordered maps?
        //answer: just change
    unordered_map<char, string> hi;
    //there we go, we have an unordered map!
    //the rest is the same!
    return 0;
}