#include <bits/stdc++.h>
using namespace std;
int main(){
    //!this doesn't work, I would have to cover all six cases(it's not just about the mix or max);
        //! a suite of if-else would be better;
    // ofstream cout("billboard.out");
    // ifstream cin("billboard.in");
    // int arr[4], arr2[4], area1, area2;
    // cin>>arr[0]>>arr[1]>>arr[2]>>arr[3];
    // cin>>arr2[0]>>arr2[1]>>arr2[2]>>arr2[3];
    // for(auto x:arr) cerr<<x;
    // cerr<<"\n";
    // area1=abs(arr[0]-arr[2])*abs(arr[1]-arr[3]);
    // area2=abs(max(arr[0], arr2[0])-min(arr2[2],arr[2]))*abs(max(arr[1], arr2[1])-min(arr[3], arr2[3]));
    // cerr<<area1<<" "<<area2;
    // cout<<(area1-area2);
 
    return 0;
}