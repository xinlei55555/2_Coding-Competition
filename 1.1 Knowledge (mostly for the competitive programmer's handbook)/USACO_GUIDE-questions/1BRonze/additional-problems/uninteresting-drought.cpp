//just simple procedure that I am suppose to deduce. (I knew it was O(n), therefore, I should've checked, and simulated what I should do at every position)
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define debug(x) cerr<<#x<<" "<<x<<"\n";

int main(){
    ll t, n,i; 
    cin>>t;
    while(t--){
        cin>>n;
        ll arr[n];
        for(i=0;i<n;i++){
            cin>>arr[i];
            
        }
    }
    return 0;
}