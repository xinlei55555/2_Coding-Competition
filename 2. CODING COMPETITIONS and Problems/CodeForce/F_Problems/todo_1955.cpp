#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
#define debug(x) cerr<<#x<<" "<<x<<"\n";

int main(){
    int T;
    cin>>T;
    while(T--){
        ll arr[4];
        cin>>arr[0]>>arr[1]>>arr[2]>>arr[3];
        // A wins if bitwise XOR of all elements in the sequence is 0
    }


    return 0;
}