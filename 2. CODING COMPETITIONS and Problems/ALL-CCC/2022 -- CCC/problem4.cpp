#include <bits/stdc++.h>

using namespace std;

int main(){
    int a,b;
    cin>>a>>b;
    int temp;
    vector<int> sample;
    for(int i = 0; i<a; i++){
        cin>>temp;
        sample.push_back(temp);
    }
    //the sum of the differences of the three points has to at least be c
    for(int i =0; i<a; i++){
        for(int i = 0; i<3; i++){
            
        }
    }
    return 0;
}